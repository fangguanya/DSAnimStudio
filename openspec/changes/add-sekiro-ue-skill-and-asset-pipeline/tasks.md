## 1. 正式资产包契约

- [ ] 1.1 为角色级正式资产包定义稳定的摘要 schema，覆盖模型、动画、PNG 纹理、材质清单、技能配置、参数关联摘要和角色级完成状态
- [ ] 1.2 收紧技能配置与材质清单 JSON schema，补齐 UE 复刻所需的材质实例键、纹理参数键、DummyPoly 解析链路、义手覆盖和战技关联字段
- [ ] 1.3 明确正式资产包与调试产物的边界，确保正式成功判定只依赖规范交付物

## 2. 导出侧实现

- [ ] 2.1 更新 `SekiroExporter` 角色导出流程，按角色生成正式资产包摘要并记录每类交付物的稳定路径和完成状态
- [ ] 2.2 更新技能配置导出逻辑，输出事件到 `BehaviorParam`、`AtkParam`、`SpEffectParam`、`EquipParamWeapon` 的正式语义链路以及 DummyPoly 覆盖结果
- [ ] 2.3 更新材质清单和纹理导出逻辑，确保每个正式材质引用都能映射到唯一 PNG 和唯一 UE 材质参数键；缺失时角色导出必须失败
- [ ] 2.4 修复动画正式导出合同，确保每个 clip 正确交付 root-motion，并在角色级摘要中记录 root-motion 来源、时间基准、轴向约定和失败原因
- [ ] 2.5 为导出侧补充角色级验证或测试，证明缺少材质、纹理、技能、参数关联或 root-motion 时会 fail-closed

## 3. UE 导入编排

- [ ] 3.1 实现或收紧 UE Python 导入脚本，使其按正式资产包导入 SkeletalMesh、纹理、材质实例、动画、技能数据资产、参数缓存和角色数据资产
- [ ] 3.2 实现正式清单驱动的材质绑定与技能绑定，禁止按文件名或目录扫描进行猜测式兜底
- [ ] 3.3 让 UE 导入链路正确消费动画 root-motion，并把 root-motion 绑定结果写入角色级导入报告
- [ ] 3.4 为导入脚本补充角色级导入报告，明确哪些资源关系和 root-motion 绑定建立成功，哪些失败并阻断角色成功结论

## 4. SekiroSkillEditor 复刻能力

- [ ] 4.1 扩展 `SekiroSkillEditor` 数据资产和加载流程，使编辑器会话消费正式导入后的角色、技能、材质和参数引用
- [ ] 4.2 在 Unreal 中实现自定义 Sekiro 时间轴编辑器，覆盖事件分轨、区间事件、播放头、属性面板和与角色预览联动的检查流程，并以 `skill_config` 作为正式序列化来源
- [ ] 4.3 在时间轴、属性面板和 3D 视口中实现 Sekiro 技能语义复刻，展示解析后的 DummyPoly、生效参数链路、义手覆盖、战技样式检查和 root-motion 结果
- [ ] 4.4 为角色会话补充材质实例、纹理绑定与 root-motion 校验，确保编辑器使用的是正式材质装配和正式动画位移结果，而非临时默认材质或临时位移重建

## 5. 角色级验收闭环

- [ ] 5.1 实现 `sekiro-ue-replica-validation` 所需的角色级验证入口，统一检查正式资产包完整性、UE 导入完整性、自定义时间轴编辑器会话和编辑器复刻结果
- [ ] 5.2 实现批量角色验收报告，逐角色输出失败阶段和失败原因，只要存在失败角色就返回整体失败
- [ ] 5.3 选取至少一个正式 Sekiro 角色样本执行端到端验证，确认模型、动画、材质、纹理、技能语义和 root-motion 都可在 UE 工程中完整复刻

## 6. 从 `formalize-sekiro-export-pipeline` 继承的未完成工作

- [ ] 6.1 建立 Sekiro 当前使用纹理编码的正式支持矩阵，并在代码中按支持矩阵判定成功/失败
- [ ] 6.2 为正式 PNG 输出定义统一命名、色彩/通道语义和材质引用契约
- [ ] 6.3 收紧 UE 导入验证标准，确保 SkeletalMesh、Skeleton、PhysicsAsset 和动画绑定全部纳入正式通过条件
- [ ] 6.4 对至少一个非主角角色执行同样的正式化回归，验证正式合同不依赖 `c0000` 特例
- [ ] 6.5 清理已经不再属于正式链路的导出选项、日志措辞和历史注释，避免误导后续开发
- [ ] 6.6 重新生成正式统计基线，替换历史上基于 fallback 和宽松成功标准得到的结论
- [ ] 6.7 验证 DSAnimStudio UI 菜单项正确显示且在正确条件下启用/禁用
- [ ] 6.8 验证技能编辑器时间轴、3D 视口、攻击判定框
- [ ] 6.9 梳理模型导出正式格式，确保默认导出只保留可导入 UE 骨骼模型的单一交付物；DAE/其他中间格式仅在 `--keep-intermediates` 下保留
- [ ] 6.10 验证 FLVER 导出结果在 UE 中确实生成 SkeletalMesh、Skeleton 和 PhysicsAsset；存在模型输入但未形成骨骼网格时必须判失败
- [ ] 6.11 修复动画导出/导入契约，确保每个 clip 是单文件完整动画，并与角色 Skeleton 正确绑定；零轨道、错 Skeleton、空动画不得计为成功
- [ ] 6.11A 修复 root-motion 导出/导入契约，确保角色位移与动画时间轴严格对齐；缺失 root-motion、错轴 root-motion、静态 root-motion 或 UE 提取结果与正式摘要不一致时必须判失败
- [ ] 6.12 让 3D 视口基于真实 DummyPoly、BehaviorParam 和 AtkParam 驱动攻击判定可视化，禁止以 Actor 原点偏移球体替代正式复刻
- [ ] 6.13 让特效点、音效点、WeaponArt、义手覆盖在属性面板和视口中显示解析后的实际生效结果，而不是仅显示原始事件类别
- [ ] 6.13A 让自定义时间轴编辑器达到原工作流必需的信息密度和交互粒度，禁止仅保留简化版浏览器/列表视图
- [ ] 6.14 为 CLI 和导入命令增加角色级验收报告，逐角色输出模型、动画、纹理、技能配置、参数表和 UE 资产导入结果，禁止只给总数；并在新验收标准下重新跑全量导出与全量导入，替换掉历史上不准确的“全部成功”结论