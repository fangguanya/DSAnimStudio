## ADDED Requirements

### 需求:技能正式导出必须输出严格 schema
技能导出能力在正式模式下必须输出严格、可验证的 schema，包含事件类型、正式字段名、正式数据类型、动画元数据和参数表关联，禁止降级为未结构化内容。

#### 场景:正式技能导出
- **当** 用户正式导出角色技能配置
- **那么** 输出文件必须满足正式 schema，并可被 UE 侧按同一合同导入

## MODIFIED Requirements

### 需求:缺模板或缺字段定义时必须显式失败
技能配置导出在缺少模板、缺少字段定义或遇到未知数据类型时必须显式失败或标记未完成映射，禁止退化为 raw bytes、占位字段名或猜测性读取。

#### 场景:模板文件缺失
- **当** 正式技能导出所需模板不存在或无法读取
- **那么** 技能导出必须失败并记录原因